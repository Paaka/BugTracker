const initalState = {
    projects:[],
};

const projectReducer = (state = initalState, action) => {
    switch(action.type){
        default :
            return state;
    };
};

export default projectReducer;